<form method="post" action="{{ path('shop_cart_delete', {'id': shop_cart.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shop_cart.id) }}">
    <button class="btn">Delete</button>
</form>
